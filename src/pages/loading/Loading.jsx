import './loading.css'
export default function Loading( {theme} ) {
      return (
            <section className={`loading ${theme}`}>
                  <span></span>
            </section>
      )
}
